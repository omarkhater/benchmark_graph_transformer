defaults:
  - transformer

task: graph
type: GraphTransformer

gnn_cfg:
  gnn_conv_type: gcn
  gnn_position: post

training:
  mlflow:
    run_name: GraphTransformer-GCN-Post
    description: |
      GraphTransformer with GNN convolution type as GCN.
      The position of GNN is set to post.
      No Bias or positional encoding is used.
