# configs/model/variants/bias.yaml
defaults:
  - transformer

encoder_cfg:
  bias:
    spatial:
      enabled: true
      num_spatial: 32
    edge:
      enabled: false
      num_edges: 16
    hop:
      enabled: false
      num_hops: 5

task: graph
type: GraphTransformer

training:
  mlflow:
    run_name: GraphTransformer-Bias-Spatial
    description: |
      GraphTransformer with spatial bias enabled.
