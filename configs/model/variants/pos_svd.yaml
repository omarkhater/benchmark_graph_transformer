defaults:
  - transformer

encoder_cfg:
  positional:
    degree:
      enabled: false
      max_degree: 32
    eig:
      enabled: false
      num_eigenc: 8
    svd:
      enabled: true
      num_svdenc: 8
task: graph
type: GraphTransformer
training:
  mlflow:
    run_name: GraphTransformer-PositionalEncoding-SVD
    description: |
      GraphTransformer with SVD encodings enabled
